package com.swein.customizecolorswitchexample

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

/**
 * Hello, let's build a customize color switch
 *
 * 1. build shape when switch off
 * - build thumbnail
 * - build track
 *
 * 2. build shape when switch on
 * - build thumbnail
 * - build track
 *
 * 3. build thumbnail selector
 * 4. build track selector
 *
 * 5. xml
 * 6. let's run app
 *
 * thanks
 *
 * youtube video url: https://youtu.be/NWQBO80I9iw
 */
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
    }
}